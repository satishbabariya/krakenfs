# KrakenFS Configuration for Node 3
log:
  level: "info"

filesystem:
  watch_paths:
    - "/data"  # Shared data directory
  exclude_patterns:
    - "*.tmp"
    - "*.log"
    - ".git"
    - "node_modules"
    - ".DS_Store"
  recursive: true
  debounce_time: "100ms"

sync:
  node_id: "node3"  # Will be overridden by NODE_ID env var
  cluster_nodes: []  # Will be overridden by CLUSTER_NODES env var
  p2p_port: 6885
  tracker_port: 6886
  bandwidth:
    enable: true
    egress_bits_per_sec: 1677721600  # 200*8 Mbit
    ingress_bits_per_sec: 2516582400  # 300*8 Mbit
  conflict_resolution:
    strategy: "timestamp"
    timeout: "30s"

volume:
  root_path: "/var/lib/krakenfs/volumes"
  driver_name: "krakenfs" 